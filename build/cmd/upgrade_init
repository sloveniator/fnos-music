#!/bin/bash
# GuSi Music — 升级初始化：清除就绪标记，升级后首启重新确认
set -u
TRIM_PKGVAR="${TRIM_PKGVAR:-/tmp/gusi-music}"
mkdir -p "${TRIM_PKGVAR}" >/dev/null 2>&1 || true
rm -f "${TRIM_PKGVAR}/runtime/rootless-ready" >/dev/null 2>&1 || true
exit 0
